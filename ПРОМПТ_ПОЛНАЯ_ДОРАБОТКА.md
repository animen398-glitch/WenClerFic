# ПРОМПТ: Полная доработка сайта WenClerFic на основе Ficbook

**Контекст:** Сайт WenClerFic (wenclerfic.onrender.com) - платформа для фанфиков в стиле Ficbook. Нужно реализовать полный функционал на основе примеров с сайта-конкурента.

**Технический стек:** Node.js + Express, SQLite, Vanilla JavaScript, HTML/CSS

---

## 🎯 ОСНОВНЫЕ ТРЕБОВАНИЯ:

### 1. ИСПРАВЛЕНИЕ ВСЕХ КНОПОК
- **Проблема:** Многие кнопки не работают на разных страницах (фанфики, профили, создание и т.д.)
- **Решение:** Все кнопки должны иметь обработчики событий и работать корректно на всех страницах

### 2. СОХРАНЕНИЕ КУКИ И СЕССИЙ
- **Проблема:** Выплывают окна "invalid" при действиях
- **Решение:** Правильно настроить сохранение куки, сессий и обработку ошибок

### 3. УБРАТЬ НЕНУЖНЫЕ ВСПЛЫВАЮЩИЕ ОКНА
- **Проблема:** Ненужные alert/confirm окна при действиях
- **Решение:** Заменить на тихие уведомления или убрать совсем

### 4. МЕНЮ АВАТАРА
- **Убрать:** "Улучшить аккаунт" и "Купить монеты"
- **Добавить:** Раскрывающиеся подменю для "Кабинет автора" и "Кабинет читателя"

---

## 📋 ДЕТАЛЬНЫЕ ТРЕБОВАНИЯ ПО СТРАНИЦАМ:

### 1. ГЛАВНАЯ СТРАНИЦА "ФАНФИКИ"

**Что должно быть:**
- Сетка обложек фанфиков (как на скриншоте)
- Фильтры в боковой панели:
  - **Направленности:** Джен, Гет, Слэш, Фемслэш, Другие виды отношений, Смешанная, Статья, Не определено
  - **Популярные метки:** Список тегов с количеством (например, "Романтика x 1785011")
- Каждая карточка фанфика должна быть кликабельной
- Кнопки "Горячая работа!" должны работать
- Пагинация должна работать

**Файлы для изменения:**
- `index.html` - добавить боковую панель с фильтрами
- `js/app.js` - добавить обработчики фильтров
- `styles/main.css` - стили для боковой панели

---

### 2. СТРАНИЦА СПИСКА ФАНФИКОВ

**Что должно быть:**
- Список фанфиков с детальной информацией:
  - Название
  - Автор
  - Вселенная/Фэндом
  - Пэйринг и персонажи
  - Размер (страницы, слова, части)
  - Дата обновления/завершения
  - Метки (теги)
  - Краткое описание
  - Кнопка "Горячая работа!"
- Боковая панель с фильтрами (как на главной)
- Пагинация

**Файлы для изменения:**
- Создать `pages/fics-list.html`
- `js/fics-list.js` - логика отображения списка
- `styles/fics-list.css` - стили

---

### 3. СТРАНИЦА "АВТОРЫ"

**Что должно быть:**
- Поиск авторов: поле "Начните вписывать имя пользователя"
- Вкладки: "За 7 дней" и "За все время"
- Список популярных авторов:
  - Рейтинг (№1, №2 и т.д.)
  - Аватар
  - Имя пользователя
  - Количество подписчиков
  - Корона (для премиум-авторов)

**Файлы для изменения:**
- Создать `pages/authors.html`
- `js/authors.js` - логика поиска и отображения
- `styles/authors.css` - стили

---

### 4. СТРАНИЦА "ПОПУЛЯРНОЕ"

**Что должно быть:**
- Список популярных фанфиков
- Фильтры по времени (сегодня, неделя, месяц, все время)
- Сортировка по популярности

**Файлы для изменения:**
- Создать `pages/popular.html`
- `js/popular.js` - логика
- `styles/popular.css` - стили

---

### 5. СТРАНИЦА "ЗАЯВКИ"

**Что должно быть:**
- Вкладки: "Всё", "Горячие", "Популярные"
- Кнопка "Добавить заявку" (красным обведена на скриншоте)
- Информационное окно о заявках
- Список заявок с:
  - Названием
  - Статусом ("Горячая заявка")
  - Метриками (лайки, избранное, комментарии)
  - Фэндомом
  - Направленностью
  - Рейтингом
  - Тегами
  - Описанием
- Боковая панель с фильтрами:
  - Тип заявки (Ориджиналы, Фанфики)
  - Дополнительно (Только с написанными работами)
  - Фэндомы
  - Метки
  - Рейтинг
  - Направленность
  - Популярность заявки
  - Дата
  - Ключевые слова

**Файлы для изменения:**
- Создать `pages/requests.html`
- `js/requests.js` - логика
- `styles/requests.css` - стили

---

### 6. ФОРМА "ДОБАВИТЬ ЗАЯВКУ"

**Что должно быть:**
- Заголовок "Добавить заявку"
- Информация "Можно добавлять:" (зеленым)
- Информация "Запрещено добавлять:" (красным)
- Поля формы:
  - Короткий заголовок (суть) заявки * (обязательное)
  - Тип: Ориджинал / Фанфик по фэндому (радио-кнопки)
  - Фэндом * (если выбран "Фанфик по фэндому")
  - Большое описание идеи
  - Дополнительное описание для авторов (спойлер)
  - Желательно одного из рейтингов * (чекбоксы: G, PG-13, R, NC-17, NC-21)
  - Желательно одной из направленностей * (чекбоксы: Джен, Гет, Слэш, Фемслэш, Другие виды отношений, Смешанная, Статья, Не определено)
  - Комментарии * (радио-кнопки: Запретить, Разрешить всем, Разрешить зарегистрированным более месяца)
  - Кнопка "Добавить"

**Файлы для изменения:**
- Создать `pages/add-request.html`
- `js/add-request.js` - логика формы
- `styles/add-request.css` - стили
- `server.js` - добавить API endpoint `/api/requests`

---

### 7. СТРАНИЦА "ДОБАВИТЬ ФАНФИК"

**Что должно быть:**
- Левая боковая панель навигации:
  - **ФАНФИК:**
    - Шапка фанфика (активная)
    - Содержание
    - Обложка
    - Отзывы к работе (0)
    - В сборниках (0)
    - Статистика
    - Страница работы
    - Мои фанфики
  - **ПРОДВИЖЕНИЕ:**
    - Добавить в «Промо»
    - Добавить в «Горячее»
- Основная форма:
  - Чекбокс "Полная версия шапки работы"
  - Название (поле ввода)
  - Обложка:
    - Информация о модерации
    - Запреты (красным)
    - Требования (размер, формат)
    - Информация о доступности
    - Кнопка "Улучшить аккаунт" (если нет доступа)
  - Конкурс (чекбокс "Участвовать в конкурсе")
  - Тип: Ориджинал / Фанфик по фэндому (радио-кнопки)
  - Рейтинг (выпадающий список: G, PG, PG-13, R, NC-17)
  - Краткое описание (текстовое поле, счетчик символов 500)
  - Подтверждение правил
  - Кнопка "Сохранить"
  - Кнопка "+ Добавить часть" (справа от заголовка)

**Файлы для изменения:**
- `pages/create.html` - обновить структуру
- `js/create.js` - обновить логику
- `styles/create.css` - обновить стили

---

### 8. СТРАНИЦА РЕДАКТИРОВАНИЯ ГЛАВЫ

**Что должно быть:**
- Левая боковая панель (как в "Добавить фанфик")
- Основная область:
  - Название части (поле ввода, например "Часть 1")
  - Текст (Rich Text Editor с панелью инструментов):
    - Форматирование: I (курсив), B (жирный), S (зачеркнутый)
    - Выравнивание: влево, по центру, вправо, по ширине
    - Отменить/Повторить
    - Tab
    - [?]
    - ***
    - Настройки
    - Импорт .docx
    - Полноэкранный режим
  - Поле для ввода текста

**Файлы для изменения:**
- `pages/add-chapter.html` - обновить
- `js/add-chapter.js` - добавить Rich Text Editor
- `styles/add-chapter.css` - стили редактора

---

### 9. СТРАНИЦА РАБОТЫ (ПРЕДПРОСМОТР)

**Что должно быть:**
- Левая колонка:
  - Информация об авторе (аватар, имя)
  - Вселенная
  - Размер
  - Публикация на других ресурсах
  - **Управление работой:**
    - Редактировать шапку
    - Добавить часть
    - Перейти в статистику
  - **Продвижение:**
    - Добавить в «Промо»
    - Добавить в «Горячее»
    - Добавить обложку
  - **Поделиться:**
    - Telegram
    - Вконтакте
    - Twitter
    - Скопировать ссылку
  - Кнопки: Отзывы (0), В сборник (0)
  - Кнопка "Добавить часть"
- Правая колонка:
  - Награды от читателей (пока нет)
- Основная область:
  - Заголовок "Содержание"
  - Список глав

**Файлы для изменения:**
- `pages/fic.html` - обновить структуру
- `js/fic.js` - обновить логику
- `styles/fic.css` - обновить стили

---

### 10. СТРАНИЦА "МОИ ФАНФИКИ"

**Что должно быть:**
- Заголовок "Мои фанфики"
- Кнопка "Добавить фанфик" (справа от заголовка)
- Вкладки:
  - Черновики (4) - активная
  - В процессе
  - Завершённые
  - Серии
- Описание: "Работы видны только вам и вашим помощникам."
- Список фанфиков:
  - Название (или "Без названия")
  - Лайки (0)
  - Ссылки: Статистика, Отзывы (0), Добавить часть
  - Кнопка меню (три точки)
  - Кнопки: "В «ПРОМО»", "В «Горячее»"

**Файлы для изменения:**
- Создать `pages/my-fics.html`
- `js/my-fics.js` - логика
- `styles/my-fics.css` - стили
- `server.js` - добавить API endpoint `/api/user/fics`

---

### 11. СТРАНИЦА "ИСТОРИЯ ИЗМЕНЕНИЙ"

**Что должно быть:**
- Вкладки: "Мои тексты", "Бета/Соавтор/Гамма"
- Заголовок "История изменений ваших текстов"
- Описание
- Правая боковая панель:
  - Мои фанфики
  - Мой блог
  - Отзывы
  - История изменений (активная)
  - Сообщения об ошибках
  - Персональный баннер
  - Кабинет читателя (раскрывающееся):
    - Сборники
    - Закладки
  - Обсуждения
  - Подписки на авторов
  - Понравившиеся работы
  - Прочитанные работы
  - История подписок
  - Просмотренные работы
  - Мои ленты
  - Кабинет помощника (раскрывающееся)

**Файлы для изменения:**
- Создать `pages/history.html`
- `js/history.js` - логика
- `styles/history.css` - стили

---

### 12. СТРАНИЦА "МОЙ ПРОФИЛЬ"

**Что должно быть:**
- Баннер профиля с кнопкой "Изменить фон"
- Аватар пользователя
- Имя пользователя
- Статус "Сейчас в сети" (зеленым)
- "На автора подписано 0 читателей."
- Кнопка "Просмотры профиля" (выпадающий список)
- Левая боковая панель:
  - Информация
  - Блог
  - Сборники (3)
  - Подарки
  - Награды
  - Отзывы
  - Достижения
- Основная область:
  - Секция "Сборники:"
    - Избранное (2) [замок] [X]
    - 00x9 [человек]
    - Прочитанные работы (1) [замок] [X]

**Файлы для изменения:**
- `pages/profile.html` - обновить
- `js/profile.js` - обновить логику
- `styles/profile.css` - обновить стили

---

### 13. ФОРМА РЕГИСТРАЦИИ

**Что должно быть:**
- Заголовок "Регистрация через email"
- Поля:
  - Email (обязательное)
  - Имя пользователя (обязательное)
    - Правила: "Можно: латинские или русские буквы, цифры, символы (дефис - нижнее подчёркивание _ точка . и пробел)."
    - "Нельзя: нецензурная лексика, одновременно латинские и русские буквы."
  - Пароль (обязательное, с иконкой показать/скрыть)
- Согласие с правилами (ссылки на Правила сайта, Пользовательское соглашение, Политику обработки персональных данных)
- Кнопка "Зарегистрироваться"

**Файлы для изменения:**
- `index.html` - обновить форму регистрации в модальном окне
- `js/app.js` - обновить валидацию

---

### 14. ФОРМА ВХОДА

**Что должно быть:**
- Заголовок "Вход"
- Поля:
  - Логин (или Email)
  - Пароль (с иконкой показать/скрыть)
- Чекбокс "Запомнить меня"
- Ссылка "Забыли пароль?"
- Кнопка "Войти"
- Разделитель "или"
- Кнопки OAuth: Google, Facebook

**Файлы для изменения:**
- `index.html` - обновить форму входа в модальном окне
- `js/app.js` - обновить логику входа

---

### 15. СТРАНИЦА "ПОИСК ФАНФИКОВ" (РАСШИРЕННЫЙ ПОИСК)

**Что должно быть:**
- Заголовок "Расширенный поиск фанфиков"
- Описание "Настройте желаемые параметры для поиска подходящих фанфиков."
- Ссылки: "Свернуть все | Развернуть все | Сбросить все"
- Боковая панель с фильтрами:
  - Тип работы (Ориджиналы, Фанфики)
  - Дополнительно (Непрочитанные, Прочитанные, Написанные по заявкам, «Горячие работы»)
  - Фэндомы (раскрывающееся)
  - Пэйринги (раскрывающееся)
  - Статус/размер (раскрывающееся)
  - Популярность работы (раскрывающееся)
  - Направленность (раскрывающееся)
- Основная область:
  - Список найденных фанфиков (как на странице списка)
  - Пагинация

**Файлы для изменения:**
- Создать `pages/search.html`
- `js/search.js` - логика поиска
- `styles/search.css` - стили

---

## 🔧 ТЕХНИЧЕСКИЕ ИСПРАВЛЕНИЯ:

### 1. ИСПРАВЛЕНИЕ ВСЕХ КНОПОК

**Файл: `js/app.js`**

```javascript
// Добавить универсальные обработчики для всех страниц
function setupUniversalHandlers() {
  // Обработчики для всех кнопок "Добавить фанфик"
  document.querySelectorAll('[data-action="add-fic"]').forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      if (!state.currentUser) {
        showAuthModal('register');
      } else {
        window.location.href = '/create';
      }
    });
  });

  // Обработчики для всех кнопок "Горячая работа"
  document.querySelectorAll('[data-action="hot-work"]').forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      // Логика добавления в "Горячее"
    });
  });

  // Обработчики для всех кнопок "В «ПРОМО»"
  document.querySelectorAll('[data-action="promo"]').forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      // Логика добавления в "Промо"
    });
  });

  // И так далее для всех типов кнопок...
}
```

### 2. СОХРАНЕНИЕ КУКИ И СЕССИЙ

**Файл: `server.js`**

```javascript
// Убедиться, что куки правильно устанавливаются
function getCookieOptions(maxAge = SESSION_DEFAULT_TTL) {
  return {
    httpOnly: true,
    secure: IS_PRODUCTION,
    sameSite: 'lax',
    maxAge,
    path: '/' // Важно: установить путь
  };
}

// Добавить middleware для обработки ошибок сессии
app.use((req, res, next) => {
  // Не показывать ошибки "invalid" пользователю
  // Логировать в консоль
  next();
});
```

**Файл: `js/session.js`**

```javascript
// Улучшить обработку ошибок
export async function syncSessionWithServer() {
  if (syncPromise) return syncPromise;

  syncPromise = (async () => {
    try {
      const response = await fetch(SESSION_ENDPOINT, {
        method: 'GET',
        credentials: 'include'
      });

      if (!response.ok) {
        if (response.status === 401) {
          clearSessionData();
          // НЕ показывать alert, просто очистить данные
          return null;
        }
        return null;
      }

      const data = await response.json();
      if (data?.user && data?.token) {
        saveSessionData(data.user, data.token);
      }
      return data;
    } catch (error) {
      console.warn('Не удалось синхронизировать сессию:', error);
      // НЕ показывать alert пользователю
      return null;
    } finally {
      syncPromise = null;
    }
  })();

  return syncPromise;
}
```

### 3. УБРАТЬ НЕНУЖНЫЕ ВСПЛЫВАЮЩИЕ ОКНА

**Файл: `js/app.js`**

```javascript
// Заменить все alert на тихие уведомления
function showNotification(message, type = 'info') {
  // Создать элемент уведомления вместо alert
  const notification = document.createElement('div');
  notification.className = `notification notification--${type}`;
  notification.textContent = message;
  document.body.appendChild(notification);
  
  setTimeout(() => {
    notification.remove();
  }, 3000);
}

// Заменить все alert() на showNotification()
// Например:
// Было: alert('Ошибка входа');
// Стало: showNotification('Ошибка входа', 'error');
```

### 4. МЕНЮ АВАТАРА С РАСКРЫВАЮЩИМИСЯ ПОДМЕНЮ

**Файл: `index.html`**

```html
<div class="avatar-menu__dropdown" id="avatar-dropdown" role="menu" aria-hidden="true">
  <div class="avatar-menu__group">
    <!-- УБРАТЬ: <button class="avatar-menu__item">Улучшить аккаунт</button> -->
    <button class="avatar-menu__item">Мои новости</button>
    <a href="/profile" class="avatar-menu__item">Мой профиль</a>
    <button class="avatar-menu__item">Личные сообщения</button>
    
    <!-- Кабинет автора с подменю -->
    <div class="avatar-menu__submenu">
      <button class="avatar-menu__item avatar-menu__item--parent" data-submenu="author">
        Кабинет автора <span class="arrow">▼</span>
      </button>
      <div class="avatar-menu__submenu-items" id="author-submenu" style="display: none;">
        <a href="/create" class="avatar-menu__item">Добавить фанфик</a>
        <a href="/my-fics" class="avatar-menu__item">Мои фанфики</a>
        <button class="avatar-menu__item">Мой блог</button>
        <button class="avatar-menu__item">Отзывы</button>
        <button class="avatar-menu__item">История изменений</button>
        <button class="avatar-menu__item">Сообщения об ошибках</button>
        <button class="avatar-menu__item">Персональный баннер</button>
      </div>
    </div>
    
    <!-- Кабинет читателя с подменю -->
    <div class="avatar-menu__submenu">
      <button class="avatar-menu__item avatar-menu__item--parent" data-submenu="reader">
        Кабинет читателя <span class="arrow">▼</span>
      </button>
      <div class="avatar-menu__submenu-items" id="reader-submenu" style="display: none;">
        <button class="avatar-menu__item">Сборники</button>
        <button class="avatar-menu__item">Закладки</button>
        <button class="avatar-menu__item">Понравившиеся работы</button>
        <button class="avatar-menu__item">Прочитанные работы</button>
      </div>
    </div>
    
    <!-- Кабинет помощника -->
    <button class="avatar-menu__item">Кабинет помощника</button>
  </div>
  <div class="avatar-menu__group">
    <!-- УБРАТЬ: <button class="avatar-menu__item">Купить монеты</button> -->
    <button class="avatar-menu__item">Заявки</button>
    <button class="avatar-menu__item">Связь</button>
    <a href="/profile/settings" class="avatar-menu__item">Настройки</a>
    <button class="avatar-menu__item avatar-menu__item--danger" id="logout-btn">Выйти</button>
  </div>
</div>
```

**Файл: `js/app.js`**

```javascript
function setupMenuHandlers() {
  // Обработчики для раскрывающихся подменю
  document.querySelectorAll('[data-submenu]').forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      e.stopPropagation();
      const submenuId = btn.dataset.submenu;
      const submenu = document.getElementById(`${submenuId}-submenu`);
      const arrow = btn.querySelector('.arrow');
      
      if (submenu) {
        const isOpen = submenu.style.display !== 'none';
        submenu.style.display = isOpen ? 'none' : 'block';
        if (arrow) {
          arrow.textContent = isOpen ? '▼' : '▲';
        }
      }
    });
  });
  
  // Остальные обработчики...
}
```

---

## 📊 БАЗА ДАННЫХ:

### Новые таблицы:

```sql
-- Таблица заявок
CREATE TABLE IF NOT EXISTS requests (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  userId INTEGER NOT NULL,
  title TEXT NOT NULL,
  type TEXT NOT NULL, -- 'original' или 'fanfic'
  fandomId INTEGER,
  description TEXT,
  spoilerDescription TEXT,
  ratings TEXT, -- JSON массив выбранных рейтингов
  directions TEXT, -- JSON массив выбранных направленностей
  commentsAllowed TEXT, -- 'forbid', 'all', 'registered_month'
  isHot BOOLEAN DEFAULT 0,
  likes INTEGER DEFAULT 0,
  favorites INTEGER DEFAULT 0,
  comments INTEGER DEFAULT 0,
  createdAt DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (userId) REFERENCES users(id) ON DELETE CASCADE
);

-- Таблица фэндомов
CREATE TABLE IF NOT EXISTS fandoms (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL UNIQUE,
  description TEXT,
  createdAt DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Таблица тегов
CREATE TABLE IF NOT EXISTS tags (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL UNIQUE,
  usageCount INTEGER DEFAULT 0,
  createdAt DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Связь фанфиков и тегов
CREATE TABLE IF NOT EXISTS fic_tags (
  ficId INTEGER NOT NULL,
  tagId INTEGER NOT NULL,
  PRIMARY KEY (ficId, tagId),
  FOREIGN KEY (ficId) REFERENCES fics(id) ON DELETE CASCADE,
  FOREIGN KEY (tagId) REFERENCES tags(id) ON DELETE CASCADE
);
```

---

## 🎨 СТИЛИ:

### Добавить в `styles/main.css`:

```css
/* Боковая панель фильтров */
.filters-sidebar {
  width: 250px;
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 20px;
  padding: 1.5rem;
  margin-right: 2rem;
}

.filters-section {
  margin-bottom: 2rem;
}

.filters-section__title {
  font-size: 1.1rem;
  font-weight: 600;
  margin-bottom: 1rem;
  color: var(--text-primary);
}

.filter-item {
  display: flex;
  align-items: center;
  padding: 0.5rem 0;
  cursor: pointer;
  color: var(--text-secondary);
  transition: color 0.2s;
}

.filter-item:hover {
  color: var(--text-primary);
}

.filter-item__count {
  margin-left: auto;
  color: var(--text-muted);
}

/* Раскрывающиеся подменю */
.avatar-menu__submenu-items {
  padding-left: 1.5rem;
  margin-top: 0.5rem;
}

.avatar-menu__item--parent {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.avatar-menu__item--parent .arrow {
  font-size: 0.8rem;
  transition: transform 0.2s;
}

/* Уведомления вместо alert */
.notification {
  position: fixed;
  top: 20px;
  right: 20px;
  padding: 1rem 1.5rem;
  background: var(--surface);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 12px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
  z-index: 10000;
  animation: slideIn 0.3s ease-out;
}

.notification--error {
  border-left: 4px solid #ef4444;
}

.notification--success {
  border-left: 4px solid #10b981;
}

@keyframes slideIn {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}
```

---

## ✅ ЧЕКЛИСТ ВЫПОЛНЕНИЯ:

- [ ] Исправлены все кнопки на всех страницах
- [ ] Настроено сохранение куки и сессий
- [ ] Убраны ненужные alert/confirm окна
- [ ] Убраны "Улучшить аккаунт" и "Купить монеты" из меню
- [ ] Добавлены раскрывающиеся подменю для "Кабинет автора" и "Кабинет читателя"
- [ ] Реализована страница "Фанфики" с боковой панелью фильтров
- [ ] Реализована страница "Авторы"
- [ ] Реализована страница "Популярное"
- [ ] Реализована страница "Заявки"
- [ ] Реализована форма "Добавить заявку"
- [ ] Обновлена страница "Добавить фанфик" с боковой панелью
- [ ] Обновлена страница редактирования главы с Rich Text Editor
- [ ] Обновлена страница работы (предпросмотр)
- [ ] Реализована страница "Мои фанфики"
- [ ] Реализована страница "История изменений"
- [ ] Обновлена страница "Мой профиль"
- [ ] Обновлены формы регистрации и входа
- [ ] Реализована страница "Поиск фанфиков"
- [ ] Созданы все необходимые таблицы в БД
- [ ] Добавлены все API endpoints
- [ ] Протестировано на всех страницах

---

## 🚀 ПРИОРИТЕТЫ:

1. **КРИТИЧНО:** Исправить все кнопки и убрать ненужные окна
2. **КРИТИЧНО:** Настроить сохранение куки и сессий
3. **ВАЖНО:** Обновить меню аватара с подменю
4. **ВАЖНО:** Реализовать основные страницы (Фанфики, Авторы, Заявки)
5. **СРЕДНЕ:** Реализовать остальные страницы

---

## 📝 ПРИМЕЧАНИЯ:

- Сохранять текущий стиль сайта (темная тема, фиолетовые акценты)
- Все формы должны иметь валидацию
- Все API запросы должны обрабатывать ошибки без показа alert
- Использовать существующую структуру проекта
- Следовать БЭМ для CSS классов
- Добавлять комментарии к новому коду

