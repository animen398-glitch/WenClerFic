# 🚀 Быстрый деплой на Vercel (5 минут)

## ⚠️ ВАЖНО: GitHub Pages НЕ ПОДДЕРЖИВАЕТ Node.js!

GitHub Pages работает только со статическими файлами (HTML, CSS, JS). 
Ваш проект использует **Node.js сервер** (Express + база данных), поэтому GitHub Pages не может его запустить.

## ✅ Решение: Используйте Vercel

Vercel **бесплатно** и автоматически подхватывает изменения из GitHub.

---

## 📋 Пошаговая инструкция:

### Шаг 1: Зайдите на Vercel
1. Откройте [vercel.com](https://vercel.com)
2. Нажмите **"Sign Up"** и войдите через **GitHub**

### Шаг 2: Подключите репозиторий
1. Нажмите **"Add New..."** → **"Project"**
2. Найдите и выберите репозиторий **`WenClerFic`**
3. Нажмите **"Import"**

### Шаг 3: Настройки проекта
Vercel автоматически определит настройки, но проверьте:

- **Framework Preset:** `Other` (или оставьте автоматическое)
- **Root Directory:** `./` (оставьте как есть)
- **Build Command:** оставьте **пустым** или `npm install`
- **Output Directory:** оставьте **пустым**
- **Install Command:** `npm install`

### Шаг 4: Переменные окружения (опционально)
Если используете OAuth (Google/Facebook), добавьте:

1. Нажмите **"Environment Variables"**
2. Добавьте:
   - `GOOGLE_CLIENT_ID` = ваш ключ
   - `GOOGLE_CLIENT_SECRET` = ваш секрет
   - `GOOGLE_REDIRECT_URI` = `https://ваш-проект.vercel.app/api/auth/google/callback`
   - (замените `ваш-проект` после первого деплоя)

### Шаг 5: Деплой
1. Нажмите **"Deploy"**
2. Подождите 1-2 минуты
3. Готово! 🎉

---

## 🌐 Ваш сайт будет доступен по адресу:

После деплоя Vercel даст вам URL вида:
- `https://wen-cler-fic.vercel.app` 
- или `https://wenclerfic-xxx.vercel.app`

**Этот URL будет работать со всеми функциями:**
- ✅ Все страницы (`/fics`, `/profile`, `/fic/:id`, и т.д.)
- ✅ База данных
- ✅ API запросы
- ✅ Авторизация

---

## 🔄 Автоматическое обновление

После первого деплоя:
- Каждый `git push` в GitHub **автоматически** запускает новый деплой на Vercel
- Не нужно ничего делать вручную!
- Просто делайте `git push` и через 1-2 минуты изменения появятся на сайте

---

## ❌ Почему GitHub Pages не работает:

GitHub Pages может только:
- ✅ Отдавать статические HTML/CSS/JS файлы
- ✅ Показывать простые сайты без сервера

GitHub Pages **НЕ МОЖЕТ**:
- ❌ Запускать Node.js сервер
- ❌ Обрабатывать API запросы
- ❌ Работать с базой данных
- ❌ Обрабатывать динамические маршруты (`/fic/:id`)

Ваш проект требует сервер, поэтому нужен Vercel (или другой хостинг с поддержкой Node.js).

---

## 🆘 Если что-то не работает:

1. **Проверьте логи:** Vercel → Deployments → выберите деплой → Logs
2. **Проверьте переменные окружения:** Settings → Environment Variables
3. **Передеплойте:** Deployments → три точки (⋮) → Redeploy

---

## 📝 Итого:

1. ✅ Зайдите на vercel.com
2. ✅ Подключите репозиторий WenClerFic
3. ✅ Нажмите Deploy
4. ✅ Готово! Сайт работает на Vercel

**Не используйте GitHub Pages** - он не поддерживает ваш проект!

