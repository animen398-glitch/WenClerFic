# Быстрый способ: Google OAuth БЕЗ поиска API

## ✅ Хорошая новость!

Для OAuth авторизации **НЕ НУЖНО** искать API в библиотеке! Можно сразу создать OAuth credentials.

## 🚀 Быстрый путь (3 шага):

### Шаг 1: Перейдите в Credentials

1. В левом меню нажмите **"APIs & Services"** (APIs и сервисы)
2. Нажмите **"Credentials"** (Учетные данные)

### Шаг 2: Создайте OAuth Client ID

1. Нажмите **"+ CREATE CREDENTIALS"** (Создать учетные данные)
2. Выберите **"OAuth client ID"**

Если появится предупреждение о настройке OAuth consent screen - переходите к Шагу 3.

### Шаг 3: Настройте OAuth Consent Screen (если нужно)

1. Нажмите **"CONFIGURE CONSENT SCREEN"** (Настроить экран согласия)
2. Выберите **"External"** (Внешний) → **"CREATE"**
3. Заполните минимально:
   - **App name**: `WenClerFic`
   - **User support email**: ваш email
   - **Developer contact**: ваш email
4. Нажимайте **"SAVE AND CONTINUE"** на всех страницах (можно пропускать ненужные)
5. На странице **"Test users"** добавьте свой email
6. Нажмите **"BACK TO DASHBOARD"**

### Шаг 4: Вернитесь к созданию OAuth Client ID

1. Снова: **"Credentials"** → **"+ CREATE CREDENTIALS"** → **"OAuth client ID"**
2. **Application type**: **"Web application"**
3. **Name**: `WenClerFic Web Client`
4. **Authorized JavaScript origins**:
   - `http://localhost:3000`
5. **Authorized redirect URIs**:
   - `http://localhost:3000/api/auth/google/callback`
6. Нажмите **"CREATE"**

### Шаг 5: Скопируйте ключи

После создания вы увидите окно с:
- **Your Client ID** - скопируйте!
- **Your Client Secret** - скопируйте! (показывается только один раз!)

## 📝 Добавьте в .env

Откройте файл `.env` в папке `WenClerFic`:

```env
GOOGLE_CLIENT_ID=ваш_client_id_здесь
GOOGLE_CLIENT_SECRET=ваш_client_secret_здесь
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback
```

## ⚠️ Важно

- **НЕ нужно** искать API в библиотеке для OAuth
- **НЕ нужно** включать Google+ API или другие API
- Можно сразу создавать OAuth credentials
- Google OAuth **БЕСПЛАТНЫЙ** для разработки

## 🎯 Если все равно просит включить API

Если Google требует включить API, то:
1. В поиске введите: **"OAuth"** или **"Google+ API"**
2. Включите найденный API
3. Продолжите создание OAuth credentials

Но обычно это **НЕ требуется**!

