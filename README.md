# WenClerFic - Платформа для фанфиков

Современная платформа для создания, чтения и обмена фанфиками, вдохновленная Ficbook.net.

## Особенности

- 📚 **Каталог фанфиков** - просмотр и поиск историй
- ✍️ **Создание фанфиков** - публикация собственных произведений
- 👤 **Профили пользователей** - личные страницы авторов
- 💬 **Комментарии** - обсуждение глав и произведений
- 🔖 **Закладки** - сохранение любимых фанфиков
- 🎨 **Современный дизайн** - темная тема с аккуратным интерфейсом
- 📱 **Адаптивный дизайн** - работает на всех устройствах

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Запустите сервер:
```bash
npm start
```

3. Откройте браузер и перейдите на:
```
http://localhost:3000
```

## Структура проекта

```
ficbook/
├── index.html          # Главная страница
├── styles/
│   └── main.css        # Основные стили
├── js/
│   └── app.js          # Клиентский JavaScript
├── server.js           # Express сервер
├── package.json        # Зависимости
└── README.md           # Документация
```

## API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход

### Фанфики
- `GET /api/fics` - Список фанфиков (с фильтрами и пагинацией)
- `GET /api/fics/:id` - Получить фанфик
- `POST /api/fics` - Создать фанфик

### Главы
- `GET /api/fics/:ficId/chapters` - Список глав
- `GET /api/fics/:ficId/chapters/:chapterId` - Получить главу

### Комментарии
- `GET /api/fics/:ficId/comments` - Список комментариев
- `POST /api/fics/:ficId/comments` - Создать комментарий

## Фильтры

- **Жанр**: romance, adventure, fantasy, drama, horror, comedy
- **Рейтинг**: G, PG, PG-13, R, NC-17
- **Сортировка**: newest, popular, rating, views

## Технологии

- Frontend: HTML5, CSS3, Vanilla JavaScript (ES6+)
- Backend: Node.js, Express
- Стиль: Темная тема с градиентами и glassmorphism эффектами

## Примечания

- Текущая версия использует in-memory хранилище для демонстрации
- Для продакшена рекомендуется использовать базу данных (MongoDB, PostgreSQL)
- Пароли должны быть захешированы (bcrypt)
- Добавить JWT токены для аутентификации
- Добавить валидацию данных на сервере

## Лицензия

MIT

