# ПРОМТ: Полное исправление проблем сайта WenClerFic

**Контекст:** Сайт WenClerFic (wenclerfic.onrender.com) - платформа для фанфиков в стиле Ficbook. Нужно исправить критические ошибки и доработать функциональность.

**Технический стек:** Node.js + Express, SQLite, Vanilla JavaScript, HTML/CSS

---

## 🚨 КРИТИЧЕСКИЕ ПРОБЛЕМЫ ДЛЯ ИСПРАВЛЕНИЯ:

### 1. ОШИБКА OAuth "invalid_client"

**Проблема:** При попытке входа через Google появляется ошибка "invalid_client".

**Причины:**
- Неправильно настроены `GOOGLE_CLIENT_ID` или `GOOGLE_CLIENT_SECRET` в `.env`
- `GOOGLE_REDIRECT_URI` не совпадает с настройками в Google Console
- Для продакшена (wenclerfic.onrender.com) нужно добавить правильный redirect URI

**Исправления:**
1. Проверить файл `.env` - должны быть установлены:
   ```
   GOOGLE_CLIENT_ID=ваш_реальный_client_id
   GOOGLE_CLIENT_SECRET=ваш_реальный_client_secret
   GOOGLE_REDIRECT_URI=https://wenclerfic.onrender.com/api/auth/google/callback
   ```
2. В Google Console добавить в "Authorized redirect URIs":
   - `https://wenclerfic.onrender.com/api/auth/google/callback`
   - `http://localhost:3000/api/auth/google/callback` (для разработки)
3. Добавить проверку наличия ключей в `server.js` перед генерацией OAuth URL (как для Facebook)
4. Добавить обработку ошибки "invalid_client" с понятным сообщением пользователю

---

### 2. ОТОБРАЖЕНИЕ ИМЕНИ ПОЛЬЗОВАТЕЛЯ

**Проблема:** В форме логина в поле Email отображается "Hiccup" вместо пустого поля или placeholder.

**Исправления:**
1. В `index.html` и других страницах с формой логина:
   - Убедиться, что поле `login-email` имеет `placeholder="Введите email"` и НЕ имеет `value="Hiccup"`
   - Проверить, что нет автозаполнения браузера, которое подставляет неправильное значение
2. После успешного входа имя пользователя должно отображаться в:
   - `#user-name` (в шапке сайта)
   - Меню аватара должно показывать `username`, а не email
3. В `js/app.js` функция `updateUserUI()` должна корректно обновлять имя пользователя

---

### 3. НЕРАБОТАЮЩИЕ КНОПКИ

**Проблема:** Многие кнопки в меню аватара и на сайте не имеют обработчиков событий.

**Кнопки, которые нужно реализовать:**

#### В меню аватара (`index.html`, `pages/*.html`):
1. **"Улучшить аккаунт"** - показывать модальное окно с информацией о премиум-аккаунте
2. **"Мои новости"** - переход на `/news` или показ списка новостей
3. **"Личные сообщения"** - переход на `/messages` или открытие модального окна
4. **"Кабинет автора"** (`data-role="author"`) - переход на `/author/cabinet` или `/my-fics`
5. **"Кабинет читателя"** (`data-role="reader"`) - переход на `/reader/cabinet` или `/bookmarks`
6. **"Кабинет помощника"** (`data-role="helper"`) - переход на `/helper/cabinet`
7. **"Мой блог"** - переход на `/blog` или `/my-blog`
8. **"Отзывы"** - переход на `/reviews` или показ отзывов пользователя
9. **"История изменений"** - переход на `/history` или показ истории изменений фанфиков
10. **"Сообщения об ошибках"** - переход на `/error-reports` или форма отправки ошибок
11. **"Персональный баннер"** - переход на `/profile/banner` или настройки баннера
12. **"Купить монеты"** - переход на `/coins` или модальное окно покупки
13. **"Заявки"** - переход на `/requests` или список заявок
14. **"Связь"** - переход на `/contact` или форма обратной связи

**Исправления:**
1. В `js/app.js` добавить обработчики для всех кнопок меню:
   ```javascript
   // Обработчики для кнопок меню аватара
   document.querySelectorAll('.avatar-menu__item[data-role]').forEach(btn => {
     btn.addEventListener('click', (e) => {
       const role = btn.dataset.role;
       if (role === 'author') {
         window.location.href = '/my-fics';
       } else if (role === 'reader') {
         window.location.href = '/bookmarks';
       } else if (role === 'helper') {
         window.location.href = '/helper/cabinet';
       }
     });
   });
   
   // Обработчики для других кнопок меню
   document.querySelectorAll('.avatar-menu__item').forEach(btn => {
     const text = btn.textContent.trim();
     if (text === 'Улучшить аккаунт') {
       btn.addEventListener('click', () => showPremiumModal());
     }
     // ... и т.д. для каждой кнопки
   });
   ```

2. Создать страницы или модальные окна для каждой функции

---

### 4. БАЗА ДАННЫХ ДЛЯ СОХРАНЕНИЯ ДЕЙСТВИЙ ПОЛЬЗОВАТЕЛЯ

**Проблема:** Нет таблицы для логирования действий пользователей (просмотры, лайки, комментарии, создание фанфиков и т.д.).

**Исправления:**

1. **Создать таблицу `user_actions` в `database.js`:**
   ```javascript
   db.run(`CREATE TABLE IF NOT EXISTS user_actions (
     id INTEGER PRIMARY KEY AUTOINCREMENT,
     userId INTEGER NOT NULL,
     actionType TEXT NOT NULL, -- 'view', 'like', 'comment', 'create_fic', 'create_chapter', etc.
     targetType TEXT, -- 'fic', 'chapter', 'comment', etc.
     targetId INTEGER,
     metadata TEXT, -- JSON строка с дополнительными данными
     createdAt DATETIME DEFAULT CURRENT_TIMESTAMP,
     FOREIGN KEY (userId) REFERENCES users(id) ON DELETE CASCADE
   )`);
   ```

2. **Добавить функции в `database.js`:**
   ```javascript
   function logUserAction(userId, actionType, targetType, targetId, metadata = null) {
     return new Promise((resolve, reject) => {
       const metaStr = metadata ? JSON.stringify(metadata) : null;
       db.run(
         `INSERT INTO user_actions (userId, actionType, targetType, targetId, metadata) VALUES (?, ?, ?, ?, ?)`,
         [userId, actionType, targetType, targetId, metaStr],
         function(err) {
           if (err) reject(err);
           else resolve({ id: this.lastID });
         }
       );
     });
   }
   
   function getUserActions(userId, limit = 50) {
     return new Promise((resolve, reject) => {
       db.all(
         `SELECT * FROM user_actions WHERE userId = ? ORDER BY createdAt DESC LIMIT ?`,
         [userId, limit],
         (err, rows) => {
           if (err) reject(err);
           else resolve(rows);
         }
       );
     });
   }
   ```

3. **Использовать логирование в `server.js`:**
   - При просмотре фанфика: `logUserAction(userId, 'view', 'fic', ficId)`
   - При лайке: `logUserAction(userId, 'like', 'fic', ficId)`
   - При создании фанфика: `logUserAction(userId, 'create_fic', 'fic', ficId)`
   - При создании главы: `logUserAction(userId, 'create_chapter', 'chapter', chapterId, { ficId })`
   - И т.д.

4. **Создать API endpoint для получения истории действий:**
   ```javascript
   app.get('/api/user/actions', async (req, res) => {
     const token = extractToken(req);
     const user = await getUserFromToken(token);
     if (!user) {
       return res.status(401).json({ error: 'Не авторизован' });
     }
     
     const actions = await db.getUserActions(user.id);
     res.json({ actions });
   });
   ```

---

### 5. ДОПОЛНИТЕЛЬНЫЕ ИСПРАВЛЕНИЯ

1. **Проверка работоспособности всех кнопок:**
   - Кнопки фильтров (`#apply-filters`) - должны работать
   - Кнопки переключения вида (`view-btn`) - должны работать
   - Кнопки пагинации - должны работать
   - Кнопка поиска - должна работать

2. **Валидация форм:**
   - Форма логина должна проверять email формат
   - Форма регистрации должна проверять все поля
   - Показывать ошибки валидации под полями

3. **Обработка ошибок:**
   - Все API запросы должны обрабатывать ошибки
   - Показывать понятные сообщения пользователю
   - Логировать ошибки на сервере

4. **Совместимость с продакшеном:**
   - Проверить, что все URL работают на `wenclerfic.onrender.com`
   - Убедиться, что CORS настроен правильно
   - Проверить, что сессии работают на продакшене

---

## 📋 ЧЕКЛИСТ ВЫПОЛНЕНИЯ:

- [ ] Исправлена ошибка OAuth "invalid_client"
- [ ] Исправлено отображение имени пользователя в форме логина
- [ ] Реализованы все кнопки меню аватара (14 кнопок)
- [ ] Создана таблица `user_actions` в БД
- [ ] Добавлено логирование действий пользователей
- [ ] Проверена работоспособность всех кнопок на сайте
- [ ] Добавлена валидация форм
- [ ] Улучшена обработка ошибок
- [ ] Протестировано на продакшене (wenclerfic.onrender.com)

---

## 📝 ДЕТАЛЬНЫЙ ПРОМТ ДЛЯ ИСПОЛНЕНИЯ:

### ЗАДАЧА 1: Исправить OAuth ошибку "invalid_client"

**Шаги:**
1. Проверить файл `.env` на наличие правильных ключей:
   ```env
   GOOGLE_CLIENT_ID=ваш_реальный_client_id_без_пробелов
   GOOGLE_CLIENT_SECRET=ваш_реальный_client_secret_без_пробелов
   GOOGLE_REDIRECT_URI=https://wenclerfic.onrender.com/api/auth/google/callback
   ```

2. В `server.js` (строка ~316) добавить проверку перед генерацией OAuth URL:
   ```javascript
   app.get('/api/auth/google', (req, res) => {
     if (!OAUTH_CONFIG.google.clientId || !OAUTH_CONFIG.google.clientSecret) {
       return res.status(400).json({ 
         error: 'Google OAuth не настроен',
         message: 'Добавьте GOOGLE_CLIENT_ID и GOOGLE_CLIENT_SECRET в файл .env'
       });
     }
     // ... остальной код
   });
   ```

3. Убедиться, что `GOOGLE_REDIRECT_URI` в `.env` точно совпадает с настройками в Google Console

4. В Google Console проверить:
   - Authorized JavaScript origins: `https://wenclerfic.onrender.com`
   - Authorized redirect URIs: `https://wenclerfic.onrender.com/api/auth/google/callback`

---

### ЗАДАЧА 2: Исправить отображение имени пользователя

**Проблема:** В форме логина в поле Email показывается "Hiccup" вместо пустого поля.

**Исправления:**
1. В `index.html`, `pages/*.html` проверить поле логина:
   ```html
   <input type="email" class="form-control" id="login-email" placeholder="Введите email" required>
   ```
   Убедиться, что НЕТ атрибута `value="Hiccup"` или автозаполнения

2. В `js/app.js` функция `updateUserUI()` должна обновлять имя в шапке:
   ```javascript
   function updateUserUI() {
     const userNameEl = document.getElementById('user-name');
     if (state.currentUser) {
       userNameEl.textContent = state.currentUser.username; // НЕ email!
     } else {
       userNameEl.textContent = 'Войти';
     }
   }
   ```

3. После успешного входа очищать форму:
   ```javascript
   document.getElementById('login-form')?.reset();
   ```

---

### ЗАДАЧА 3: Реализовать все кнопки меню аватара

**Файлы для изменения:** `js/app.js`, создать `js/menu-handlers.js`

**Код для добавления в `js/app.js` после `setupEventListeners()`:**

```javascript
function setupMenuHandlers() {
  // Обработчики для кнопок "Кабинет"
  document.querySelectorAll('.avatar-menu__item[data-role]').forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      const role = btn.dataset.role;
      if (role === 'author') {
        window.location.href = '/my-fics';
      } else if (role === 'reader') {
        window.location.href = '/bookmarks';
      } else if (role === 'helper') {
        alert('Кабинет помощника - в разработке');
        // window.location.href = '/helper/cabinet';
      }
    });
  });

  // Обработчики для остальных кнопок меню
  document.querySelectorAll('.avatar-menu__item').forEach(btn => {
    const text = btn.textContent.trim();
    const href = btn.getAttribute('href');
    
    // Пропускаем кнопки, которые уже имеют href (ссылки)
    if (href) return;
    
    // Пропускаем кнопки с data-role (уже обработаны выше)
    if (btn.dataset.role) return;
    
    // Пропускаем кнопку "Выйти" (уже обработана)
    if (btn.id === 'logout-btn') return;
    
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      handleMenuClick(text, btn);
    });
  });
}

function handleMenuClick(menuText, button) {
  switch(menuText) {
    case 'Улучшить аккаунт':
      showPremiumModal();
      break;
    case 'Мои новости':
      window.location.href = '/news';
      break;
    case 'Мой профиль':
      window.location.href = '/profile';
      break;
    case 'Личные сообщения':
      window.location.href = '/messages';
      break;
    case 'Добавить фанфик':
      if (!state.currentUser) {
        showAuthModal('register');
      } else {
        window.location.href = '/create';
      }
      break;
    case 'Мои фанфики':
      window.location.href = '/my-fics';
      break;
    case 'Мой блог':
      window.location.href = '/blog';
      break;
    case 'Отзывы':
      window.location.href = '/reviews';
      break;
    case 'История изменений':
      window.location.href = '/history';
      break;
    case 'Сообщения об ошибках':
      window.location.href = '/error-reports';
      break;
    case 'Персональный баннер':
      window.location.href = '/profile/banner';
      break;
    case 'Купить монеты':
      showCoinsModal();
      break;
    case 'Заявки':
      window.location.href = '/requests';
      break;
    case 'Связь':
      window.location.href = '/contact';
      break;
    case 'Настройки':
      window.location.href = '/profile/settings';
      break;
    default:
      console.log('Неизвестная кнопка меню:', menuText);
      alert(`${menuText} - функция в разработке`);
  }
}

function showPremiumModal() {
  alert('Премиум-аккаунт дает:\n- Приоритетная поддержка\n- Расширенные возможности\n- Без рекламы\n\nСкоро будет доступно!');
}

function showCoinsModal() {
  alert('Система монет скоро будет доступна!');
}
```

**Вызвать в `setupEventListeners()`:**
```javascript
function setupEventListeners() {
  // ... существующий код ...
  setupMenuHandlers(); // Добавить эту строку
}
```

---

### ЗАДАЧА 4: Создать таблицу для логирования действий пользователей

**Файл:** `database.js`

**Добавить в функцию `initDatabase()` после создания таблицы `pending_profiles`:**

```javascript
// Таблица действий пользователей
db.run(`CREATE TABLE IF NOT EXISTS user_actions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  userId INTEGER NOT NULL,
  actionType TEXT NOT NULL,
  targetType TEXT,
  targetId INTEGER,
  metadata TEXT,
  createdAt DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (userId) REFERENCES users(id) ON DELETE CASCADE
)`, (err) => {
  if (err) {
    console.error('Ошибка создания таблицы user_actions:', err);
  } else {
    console.log('Таблица user_actions создана');
  }
});
```

**Добавить функции в конец `database.js`:**

```javascript
// User Actions
function logUserAction(userId, actionType, targetType = null, targetId = null, metadata = null) {
  return new Promise((resolve, reject) => {
    const metaStr = metadata ? JSON.stringify(metadata) : null;
    db.run(
      `INSERT INTO user_actions (userId, actionType, targetType, targetId, metadata) VALUES (?, ?, ?, ?, ?)`,
      [userId, actionType, targetType, targetId, metaStr],
      function(err) {
        if (err) {
          console.error('Ошибка логирования действия:', err);
          reject(err);
        } else {
          resolve({ id: this.lastID });
        }
      }
    );
  });
}

function getUserActions(userId, limit = 50) {
  return new Promise((resolve, reject) => {
    db.all(
      `SELECT * FROM user_actions WHERE userId = ? ORDER BY createdAt DESC LIMIT ?`,
      [userId, limit],
      (err, rows) => {
        if (err) reject(err);
        else resolve(rows);
      }
    );
  });
}

function getUserActionsByType(userId, actionType, limit = 50) {
  return new Promise((resolve, reject) => {
    db.all(
      `SELECT * FROM user_actions WHERE userId = ? AND actionType = ? ORDER BY createdAt DESC LIMIT ?`,
      [userId, actionType, limit],
      (err, rows) => {
        if (err) reject(err);
        else resolve(rows);
      }
    );
  });
}

module.exports = {
  // ... существующие экспорты ...
  logUserAction,
  getUserActions,
  getUserActionsByType
};
```

**Использовать в `server.js`:**

1. При просмотре фанфика (в `app.get('/api/fics/:id')`):
```javascript
const user = await getUserFromToken(extractToken(req));
if (user) {
  await db.logUserAction(user.id, 'view', 'fic', fic.id);
}
```

2. При создании фанфика (в `app.post('/api/fics')`):
```javascript
await db.logUserAction(user.id, 'create_fic', 'fic', newFic.id);
```

3. При создании главы (в `app.post('/api/fics/:id/chapters')`):
```javascript
await db.logUserAction(user.id, 'create_chapter', 'chapter', newChapter.id, { ficId: ficId });
```

4. Добавить API endpoint для получения истории:
```javascript
app.get('/api/user/actions', async (req, res) => {
  try {
    const token = extractToken(req);
    const user = await getUserFromToken(token);
    if (!user) {
      return res.status(401).json({ error: 'Не авторизован' });
    }
    
    const actionType = req.query.type;
    const limit = parseInt(req.query.limit) || 50;
    
    let actions;
    if (actionType) {
      actions = await db.getUserActionsByType(user.id, actionType, limit);
    } else {
      actions = await db.getUserActions(user.id, limit);
    }
    
    res.json({ actions });
  } catch (error) {
    console.error('Error getting user actions:', error);
    res.status(500).json({ error: 'Ошибка сервера' });
  }
});
```

---

### ЗАДАЧА 5: Проверить работоспособность всех кнопок

**Проверить следующие элементы:**

1. **Кнопки фильтров** (`#apply-filters`) - должны работать ✅
2. **Кнопки переключения вида** (`.view-btn`) - должны работать ✅
3. **Кнопки пагинации** - должны работать ✅
4. **Кнопка поиска** - должна работать ✅
5. **Кнопки в карточке фанфика:**
   - `#like-btn` - добавить обработчик
   - `#bookmark-btn` - добавить обработчик
   - `#share-btn` - добавить обработчик

**Добавить в `js/fic.js`:**

```javascript
// Обработчики кнопок действий
document.getElementById('like-btn')?.addEventListener('click', async () => {
  if (!currentUser) {
    showAuthModal('login');
    return;
  }
  await toggleLike();
});

document.getElementById('bookmark-btn')?.addEventListener('click', async () => {
  if (!currentUser) {
    showAuthModal('login');
    return;
  }
  await toggleBookmark();
});

document.getElementById('share-btn')?.addEventListener('click', () => {
  shareFic();
});

async function toggleLike() {
  // Реализация лайка
}

async function toggleBookmark() {
  // Реализация закладки
}

function shareFic() {
  if (navigator.share) {
    navigator.share({
      title: currentFic.title,
      text: currentFic.description,
      url: window.location.href
    });
  } else {
    // Fallback - копировать в буфер обмена
    navigator.clipboard.writeText(window.location.href);
    alert('Ссылка скопирована в буфер обмена!');
  }
}
```

---

## ✅ ПРИОРИТЕТЫ:

1. **КРИТИЧНО:** Исправить OAuth ошибку (блокирует вход)
2. **КРИТИЧНО:** Исправить отображение имени пользователя
3. **ВАЖНО:** Реализовать кнопки меню (улучшает UX)
4. **ВАЖНО:** Добавить логирование действий (для аналитики)
5. **СРЕДНЕ:** Проверить все кнопки на сайте

---

## 🧪 ТЕСТИРОВАНИЕ:

После исправлений проверить:
1. Вход через Google работает без ошибок
2. Имя пользователя отображается правильно
3. Все кнопки меню аватара работают
4. Действия пользователей логируются в БД
5. Все интерактивные элементы работают

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ:

**Файлы для изменения:**
- `server.js` - исправление OAuth, добавление логирования
- `database.js` - добавление таблицы и функций для действий пользователей
- `js/app.js` - добавление обработчиков для кнопок меню
- `index.html`, `pages/*.html` - проверка форм и кнопок
- `.env` - проверка настроек OAuth

**Стиль кода:**
- Использовать существующую структуру проекта
- Следовать БЭМ для CSS классов
- Добавлять комментарии к новому коду
- Обрабатывать все возможные ошибки

